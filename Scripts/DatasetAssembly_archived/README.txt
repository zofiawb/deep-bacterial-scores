Note on this folder - this is a record of work completed and not suitable to be run. Datasets were assembled on local machine as CSVs, with columns as different study's datasets, and cells within each column as tuples of (genomic position1, genomic position2, structure score). Datasets were too large to be combined into all species csv and so were processed separately: see the processing scripts folder for more. 